modules:
  - name: if_mib
    walk:
      - ifDescr
      - ifInOctets
      - ifOutOctets
      - ifInErrors
      - ifOutErrors
    ignore_device_errors: true
    retries: 3
    timeout: 2s
    auth:
      community: public
